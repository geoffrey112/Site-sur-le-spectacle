
{% block body %}

	<div class="container">
		{{ form_start(categoryForm) }}
		<div class="form-group mt-5">
			<div class="row">
				<div class="col-4">
					{{ form_row(categoryForm.name, {'attr': {class : "form-control"}}) }}
				</div>
			</div>
			
			<div class="row">
				{{ form_row(categoryForm.ajouter, {'attr' : {class : "btn btn-primary"}}) }}
			</div>
		</div>

		{{ form_end(categoryForm) }}
	</div>


{% endblock %}
