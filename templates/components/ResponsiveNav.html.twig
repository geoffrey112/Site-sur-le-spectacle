<div class="pos-f-t contentNav">
    <div class="collapse" id="navbarToggleExternalContent">
        <div class="bg-dark p-4 text-center">
            <h5 class="text-white h4 mb-3">Mes réalisations:</h5>
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link pageNavActive" href="{{ path("home")}}">Accueil</a>
                </li>
                {% for category in categories %}
                    <li class="nav-item">
                        <a class="nav-link pageNav" href="{{ path("category", {name:category.name}) }}">{{category.name}}</a>
                    </li>
                {% endfor %}
                <li class="nav-item">
                    <a class="nav-link pageNav" href="{{ path("dashboard")}}">Dashboard</a>
                </li>
                 <li class="nav-item">
                    {% if app.user %}
                        <a class="nav-link pageNav" href="{{ path('logout') }}" >Se déconnecter</a>
                    {% endif %}
                </li>
            </ul>
        </div>
    </div>
    <nav class="navbar navbar-light">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarToggleExternalContent" aria-controls="navbarToggleExternalContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
    </nav>
</div>